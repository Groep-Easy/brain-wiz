## What does this PR do?
<!-- One sentence. -->

## Which context does it touch?
- [ ] Server (`src/server/`)
- [ ] Host display (`src/host/`)
- [ ] Phone client (`src/client/`)
- [ ] Shared contracts (`src/shared/`) ← requires @git-master review
- [ ] Config / tooling
- [ ] Docs
- [ ] Tests

## Checklist
- [ ] I ran `npm run lint` locally — zero errors
- [ ] I ran `npm run format:check` locally — zero errors
- [ ] New logic has a corresponding test in `tests/`
- [ ] I did NOT edit files outside my squad's folder (unless this is explicitly a shared change)
- [ ] If I changed `src/shared/`, I updated all three consumers and confirmed nothing broke

## How to test
<!-- Steps a reviewer can follow to verify this works. -->
