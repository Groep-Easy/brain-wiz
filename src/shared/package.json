{
  "name": "@brain-wiz/shared",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "//exports": "The exports map serves only compiled dist/* (for tsc + Node). Browser-only assets (.css/.mp3/.svg) are resolved by Vite via resolve.alias to source and must NOT be imported from Node-resolved packages (server/config/minigames/tests), or Node throws ERR_PACKAGE_PATH_NOT_EXPORTED.",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "class-validator": "^0.15.1",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "react": "^19.2.7"
  }
}
