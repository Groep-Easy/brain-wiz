/**
 * @file App.tsx
 * @owner host-squad
 * @description Root component for the host display. Placeholder hello-world
 * screen — the host is a read-only subscriber; socket wiring and the live
 * game-state views land in a later slice.
 */
export function App(): React.JSX.Element {
  return (
    <main className="app">
      <h1>Brain Wis</h1>
      <p>Host display — scaffold ready.</p>
    </main>
  )
}
