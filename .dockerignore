.env
.env.example
.editorconfig
.prittierignore
.prittierrc

.git
.git/**
.gitattributes
.gitignore

.github
.github/**

.vscode
.vscode/**

# Build artifacts — the image compiles a fresh dist via `npm run build`.
# Never copy a host-built dist in: stale compiled files (e.g. removed
# migrations) would otherwise ship and be picked up at runtime.
dist
dist/**
node_modules
node_modules/**
